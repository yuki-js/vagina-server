# Kubernetes Production Secret Configuration Template
#
# Create the real secret-backed file from this template and mount it as
# /deployments/config/application-secret.yaml.
# Never commit actual secret values.

quarkus:
  datasource:
    username: ""
    password: ""

vagina:
  auth:
    oidc:
      providers:
        google:
          client-secret: ""
        github:
          client-secret: ""

  realtime:
    models:
      realtime:
        api-key: ""
      chat-completion:
        api-key: ""

  text-agent:
    models:
      gpt-5.5:
        api-key: ""
      aok:
        api-key: ""

  object-storage:
    access-key: ""
    secret-key: ""
